#define HTCOMPASSCALIBRATE 0x43
#define HTCOMPASSMEASURE 0x00

void HTCompassSetMode(byte port, byte mode)
{
    while(I2CCheckStatus(port)); //== until NO_ERR
    WriteI2CRegister(port, I2C_ADDR_DEFAULT, I2C_REG_CMD, mode);
}
